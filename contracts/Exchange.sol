contract Exchange {

function stake  (address _addrToken, uint _amount) public returns (uint _angAmount)
{}
function withdraw (address _addrToken, uint _amount, address _to ) public returns (uint _tokenAmount)
{}
function setPriceOracle(address _addr)wner) public onlyOwner
{}
function setOwner(address _addr) public onlyOwner
{}
}
